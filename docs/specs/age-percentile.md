# 仕様書: 年齢パーセンタイル（仮）「日本であなたは何番目？」

> IDEAS #62（公的統計 × 自分ごと化）の第1弾。
> **状態: G1・G3 の回答待ち（実装は未着手）**
> 記号: 🔴=オーナーが決める / 🔵=Claude が決めて報告 / ⬛=既存資産で決定済み / ⬜=v1ではやらない

---

# 📌 未決リスト（ここだけ答えれば実装に進めます）

開発サイクルはゲート方式で進めます。**G1・G3・G6 では必ず止まります。**

```
[G1] Discovery : 完成・成功の定義 / ターゲット        ← 🔴 いまここ
[G2] Define    : スコープ / データ出典・更新方針       ← 🔵 済（データ調達だけ要オーナー、下記）
[G3] Design    : デザイン方針 / 結果項目 / 名前・URL   ← 🔴 いまここ
[G4] Build     : 純粋関数 + テスト + 画面             ← 🔵
[G5] Verify    : テスト green / ブラウザ確認 → 実機    ← 🔵 → 🔴
[G6] Release   : リリース基準 / 公開ブランチ           ← 🔴
[G7] Operate   : Search Console で計測 → 改善          ← 🔴登録 → 🔵
```

| # | ゲート | 決めること | 推奨（そのままでよければ「推奨で」と一言） |
|---|---|---|---|
| 1 | G1 | **何をもって完成とするか / 成功とするか** | リリース判断は「自分が触って面白いか」。数字（Search Console）は3ヶ月後の振り返りに使う |
| 2 | G1 | ターゲットを絞るか | 絞らない（入力1つで誰でも使える） |
| 3 | G3 | **デザインの方向** | 案B＝結果部分だけ作り込む（サイトの一貫性は保つ） |
| 4 | G3 | 同学年（早生まれ）・男女別を出すか | どちらも入れる |
| 5 | G3 | ファイル名・タイトル | `age-rank.html` /「日本であなたは何番目？」 |
| 6 | G6 | どのブランチで作り、どこへ公開するか | 独立ブランチで作る（AdSense対策版のマージ判断を待たない） |
| 7 | G2 | **人口データをどう入手するか** ← 下の「データ調達の結果」参照。**これだけは推奨で進められない** | 案1（オーナーが CSV を1つ置く） |

---

## 📊 データ調達の結果（2026-07-24 実際に試行）

**結論: この開発コンテナからは政府統計サイトに接続できません。**

| 試したこと | 結果 |
|---|---|
| `https://www.e-stat.go.jp/` | ✗ 403（ネットワークポリシーによる遮断） |
| `https://www.stat.go.jp/` | ✗ 403（同上） |
| `https://dashboard.e-stat.go.jp/` | ✗ 403（同上） |

環境のネットワークポリシーが許可ホストを絞っているため、curl / WebFetch とも到達できません。
**数値を推測で埋めることはしません**（公的統計として提示するデータを捏造することになるため）。

### 選べる案

- **案1（推奨）: オーナーが CSV を1つリポジトリに置く**
  e-Stat の「人口推計」→ 最新年の**年齢各歳別人口（男女別）**を CSV でダウンロードし、
  `web-tools/data/population-raw.csv` として置く（またはチャットに貼る）。以降の整形は Claude が行う。
  所要 5〜10分。**最も正確で、更新も年1回この操作を繰り返すだけ**。
- **案2: 5歳階級の概算で暫定リリース**
  検索で得られる公表値（5歳階級）から暫定データを作り、画面に「5歳階級による概算」と明記する。
  精度は落ちるが、オーナーの作業ゼロで公開まで進める。後から案1のデータに差し替え可能。
- **案3: データ待ちにせず、先に実装だけ進める（案1・2と併用可）**
  データを外から差し込める形（`js/lib/population-data.mjs` が単なるデータ、ロジックは別）で作り、
  **サンプルデータで純粋関数とテストと画面を完成させておく**。実データが来たらファイル差し替えだけで公開できる。
  → **案3で実装を進め、公開前に案1のデータを入れる**のが、待ち時間ゼロで精度も落とさない進め方。

### 設計への反映（どの案でも共通）
- データは `js/lib/population-data.mjs` に**データだけ**を置き、計算ロジックは `js/lib/age-rank.mjs` に分離する。
  → データ差し替えがファイル1つの置換で済み、テストはサンプルデータで独立に回せる。
- 画面には**出典・基準日・粒度（各歳別か5歳階級か）**を必ず表示する。

---

## 1. Discovery（なぜ作るか）

### 1-1. 課題・仮説 ⬛
- 「答え型ツール」は AI チャットに需要を侵食される（2026-06-10 戦略修正）。
  攻めは**体験型**＝「自分を測る・遊ぶ・シェアする」。
- AdSense 監査で唯一「独自データ」と評価されたのが birth-year の統計。
  **公的統計 × 自分ごと化**が当サイトで実証済みの唯一の勝ち筋。
- 仮説: 「日本人の平均年齢は◯歳」は AI が答える。
  **「あなたより年上は◯◯万人」は AI が即答できず、その場で計算して見せる体験になる。**

### 1-2. 競合 ⬛
- 年齢計算ツールは飽和（当サイトの age.html 含む）。だが**人口の中の位置を出すもの**はほぼ無い。
- 人口統計サイト（政府・自治体）は表の提示のみで、自分ごと化しない。

### 1-3. ターゲット 🔴
- 案: ①SNSで話題を共有したい層（20〜40代） ②自分の年齢を意識するイベント時（誕生日・年度替わり）
- **決めること: 主ターゲットを1つに絞るか、絞らないか。**

### 1-4. 成功の定義 / 完成の定義 🔴 ★最重要
> **これが無いのが mini-tools が終わらない原因。** 今回は必ず先に決める。
- 「完成」= 何が満たされたら手を止めるか（例: リリース基準セクションの全項目 green）
- 「成功」= 公開後に何が起きたら成功か（例: Search Console でインプレッションが立つ／
  自分がスマホで見て面白いと思う／誰かに共有したくなる）
- **決めること: 成功指標を「数字」で置くか「自分の実感」で置くか。**
  - 案A: 数字（公開30日でインプレッション◯以上）→ 客観的だがトラフィックが少ないと判定不能
  - 案B: 実感（自分が友人に送りたくなるか）→ 検証器としては弱いが、この規模では現実的
  - 案C: 両方（実感でリリース判断、数字は3ヶ月後の振り返りに使う）← 推奨

---

## 2. Define（何を作るか）

### 2-1. ユーザーストーリー 🔵
1. 生年月日を入れると、日本の人口の中で自分がどの位置かがすぐ分かる。
2. 「同い年は何人いるか」「同学年は何人か」が分かる。
3. 結果をワンタップでコピーして SNS に貼れる。

### 2-2. スコープ（v1でやること）🔴確認
- 生年月日（または年齢）入力 → 人口内の位置を表示
- 出力項目（下の 3-2 参照）
- 結果テキストのコピー / X 共有リンク
- 統計の早見表を**静的HTML**で出力（SEO・#62 注意②）
- 解説（この統計は何か・出典・注意）＋ FAQ ＋ 関連ツールリンク

### 2-3. 非スコープ（v1でやらないこと）⬜
- シェア**画像**生成（Canvas）→ IDEAS #38 が来てから
- 都道府県別 / 職業別 / 年収など他統計との掛け合わせ → 第2弾以降
- 将来推計（何年後に上位◯%になるか）→ 面白いが v1 では出さない
- ログイン・保存・履歴

### 2-4. データ 🔵→🔴
- 出典: 総務省統計局「人口推計」年齢各歳別人口（男女別）。
- 形式: `js/lib/population-data.mjs` に**静的データとして同梱**（外部API依存なし・0円・端末内処理を維持）。
- 更新: 年1回、このファイルの差し替えのみで完了する設計（#37 の「毎年更新で放置が崩れる」を回避）。
- **入手は未解決**（この環境から政府統計サイトへ到達不可。冒頭の「データ調達の結果」参照）。
  → 案3（サンプルデータで実装を完成させ、公開前に実データへ差し替え）を前提に設計する。

---

## 3. Design（どう見せるか）

### 3-1. デザインの方向 🔴 ★
既存サイトは実用ツール向けの落ち着いたデザイン（`style.css`: 白カード・青アクセント・最大幅720px）。
本ツールは**体験型で、結果画面がシェアされる＝それ自体が商品**。ここで方針が分かれる。

- **案A: 既存デザインに完全に合わせる** … サイトの一貫性◎・工数最小。ただし地味でシェアされにくい。
- **案B: 結果部分だけ作り込む**（大きな数字・視覚化バー・強調色）… 一貫性を保ちつつ体験型らしさを出す。**推奨**
- **案C: このページだけ別デザイン** … インパクト最大だが、サイトの統一感とAdSense的な「まとまり」を損なう。

**決めること: A / B / C のどれか。**

### 3-2. 結果画面に出す項目 🔴
研究知見（IDEAS #62 関連）を反映した案:
1. **「同世代100人のうち、◯人があなたより年上」** ← ％でなく**自然頻度**で出す（Gigerenzer。％は理解されにくい）
2. あなたより年上は **◯◯万人** / 年下は **◯◯万人**
3. 同い年は約 **◯◯万人**
4. **同学年**は約 **◯◯万人**（早生まれ考慮）← 日本特化
5. 男女別の内訳
6. 「日本の人口のうち、上から約 **◯◯番目**」← インパクト枠（Peak-End：**最後に一番強い数字**を置く）

**決めること: ④同学年 と ⑤男女別 を入れるか。**（推奨: どちらも入れる）

### 3-3. 情報設計 🔵
入力 → 結果（上記の順）→ コピー/共有 → 解説 → 早見表 → FAQ → 関連ツール
※ 結果は入力と同時にリアルタイム更新（差別化原則②）

### 3-4. コピー・文言 🔵
- 断定的に書く（IDEAS #66「診断系は断定的に、実害系は補助的に」）。
- ただし**統計の限界は明記**（推計値である・基準日・概算である旨）。

### 3-5. アクセシビリティ / レスポンシブ 🔵
- ラベルと入力の関連付け、キーボード操作、コントラスト比、スマホ幅での可読性。既存ページと同水準。

### 3-6. 名前とURL 🔴
- 案: `age-rank.html`（分かりやすい） / `population-rank.html`（正確） / `age-percentile.html`（検索向き）
- ページタイトル案: 「日本であなたは何番目？ 年齢の人口ランキング」
- **決めること: ファイル名とタイトル。**（推奨: `age-rank.html`）

---

## 4. Build（どう作るか）🔵

既存の型に完全に従う。
- ロジックは `js/lib/age-rank.mjs` に**純粋関数**として実装
  （パーセンタイル算出 / 累積人口 / 学年判定 / 自然頻度への変換 / 数値の丸めと表記）
- 画面は `age-rank.html`（既存 `style.css` を利用、必要なら追記）
- データは `js/lib/population-data.mjs`
- 単体テスト `test/age-rank.test.mjs`

## 5. Verify（どう確かめるか）
- 🔵 単体テスト green（既存240件を壊さない）
- 🔵 ヘッドレスブラウザで表示・入力・結果更新・コピーを確認
- 🔵 リンク切れ・meta・canonical・sitemap 追加を確認
- 🔴 **オーナーがスマホで実際に触って「面白いか」を判断**（# 自宅での検証待ち に該当しない＝すぐできる）

## 6. Release 🔴
### リリース基準（これが揃ったら出す）
- [ ] 単体テスト green
- [ ] ブラウザ表示確認済み
- [ ] 出典・基準日・概算である旨の表示
- [ ] 解説・FAQ・関連リンクあり（AdSense 対策）
- [ ] sitemap.xml に追加
- [ ] オーナーがスマホで確認して OK

### 公開の判断 🔴 ★
**決めること: どのブランチに載せるか。**
- 現在 `main` = 19ツール版。AdSense 対策版（6本厳選）は `claude/adsense-approval-review-7p143j` に**未マージで待機中**。
- 案A: AdSense 対策ブランチに載せる … 路線が統一される。ただしそのブランチの main 反映判断が先に要る。
- 案B: main に直接載せる … すぐ公開できるが、AdSense 対策版とのマージが後で面倒になる。
- 案C: 独立ブランチで作り、公開先は後で決める … **推奨**（作る手を止めない）

## 7. Operate（公開後）
- 🔴 Search Console 登録（未実施）→ 以後の計測は 🔵
- 🔵 週次で数字を見て、当たったら解説を厚くする（既存の運用方針）
- 🔵 データは年1回更新

---

## オーナー判断のまとめ（6件）
| # | 決めること | 推奨 |
|---|---|---|
| 1 | **成功・完成の定義** | 案C（実感でリリース判断、数字は3ヶ月後の振り返り） |
| 2 | ターゲットを絞るか | 絞らない（入力1つで誰でも使える） |
| 3 | **デザインの方向** | 案B（結果部分だけ作り込む） |
| 4 | 同学年・男女別を入れるか | 両方入れる |
| 5 | ファイル名・タイトル | `age-rank.html` / 「日本であなたは何番目？」 |
| 6 | どのブランチで作るか | 案C（独立ブランチ、公開先は後で決める） |
